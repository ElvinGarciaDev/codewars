function mouthSize(animal) {
  // conditional. Check if animal passed in is an alligator
  if (animal.toLowerCase() == "alligator") {
    // return "wide"
    return "small";
  } else {
    return "wide";
  }
}
