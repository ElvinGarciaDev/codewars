function warnTheSheep(queue) {

    // Reverse the array
    let arr = queue.reverse()

    return arr[0] === 'wolf' ? "Pls go away and stop eating my sheep" : `Oi! Sheep number ${wolfindex = arr.indexOf('wolf')}! You are about to be eaten by a wolf!`

}